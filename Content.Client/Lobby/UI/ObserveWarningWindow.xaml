<controls:FancyWindow xmlns:orbitraKeyboard="clr-namespace:Content.Client._Orbitra.UserInterface"
    xmlns="https://spacestation14.io"
    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
    xmlns:cc="clr-namespace:Content.Client.Administration.UI.CustomControls">
    <!-- Orbitra-Edit - клавиатурные действия используют штатные обработчики. -->
    <BoxContainer Orientation="Vertical">
        <Label Text="{Loc 'observe-warning-1'}"/>
        <Label Text="{Loc 'observe-warning-2'}"/>
        <BoxContainer Orientation="Horizontal">
            <orbitraKeyboard:OrbitraButton Name="NevermindButton" Text="{Loc 'observe-nevermind'}" SizeFlagsStretchRatio="1"/>
            <Control HorizontalExpand="True" SizeFlagsStretchRatio="2" />
            <cc:CommandButton Command="observe" Name="ObserveButton" StyleClasses="negative" Text="{Loc 'observe-confirm'}" SizeFlagsStretchRatio="1"/>
            <cc:CommandButton Command="observe admin" Name="ObserveAsAdminButton" Text="{Loc 'observe-as-admin'}" SizeFlagsStretchRatio="1" Visible="False"/>
        </BoxContainer>
    </BoxContainer>
</controls:FancyWindow>
