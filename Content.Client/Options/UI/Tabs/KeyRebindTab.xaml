<Control xmlns="https://spacestation14.io"
         xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
         xmlns:orbitra="clr-namespace:Content.Client._Orbitra.Lobby">
    <BoxContainer Orientation="Vertical">
        <ScrollContainer VerticalExpand="True">
            <BoxContainer Name="KeybindsContainer" Orientation="Vertical" Margin="8 8 8 8">

                <!-- Keybind buttons are added here in the C# code -->

            </BoxContainer>
        </ScrollContainer>
        <controls:StripeBack HasBottomEdge="False" HasMargins="False">
            <orbitra:OrbitraAdaptiveRow Margin="8" SeparationOverride="8"> <!-- Orbitra-Edit -->
                <Control MinSize="8 0" />
                <Label Text="{Loc 'ui-options-binds-explanation'}" StyleClasses="LabelSubText" ClipText="True" HorizontalExpand="True" />
                <Button Name="ResetAllButton"
                        Text="{Loc 'ui-options-binds-reset-all'}"
                        HorizontalExpand="True"
                        HorizontalAlignment="Right" />
            </orbitra:OrbitraAdaptiveRow>
        </controls:StripeBack>
    </BoxContainer>
</Control>
